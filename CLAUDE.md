# Project: Pilnas Bakas (dk)

Lithuanian fuel price map — GitHub Pages static site.

## Rules

- Always bump `CACHE_VERSION` in `docs/sw.js` when changing any file under `docs/` (HTML, JS, CSS). This ensures the service worker cache is invalidated and users get updates immediately.
